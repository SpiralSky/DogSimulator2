/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/DogSimulator2",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
};

export default nextConfig;
